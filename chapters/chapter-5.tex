\section{Protocollo sperimentale}\label{sec:protocollo-sperimentale}

\subsection{Ambiente hardware e software}

\begin{table}[H]
  \centering
  \caption{Configurazione dell'ambiente sperimentale.}
  \label{tab:setup-sperimentale}
  \begin{tabular}{@{}ll@{}}
    \toprule
    \textbf{Componente} & \textbf{Valore} \\
    \midrule
    OS & Arch Linux, kernel 7.0.12, x86\_64 \\
    JDK & OpenJDK 21.0.11 \\
    Scala & 2.13.18 \\
    Spark & 4.1.1 (\texttt{local[*]}) \\
    Delta Lake & 4.1.0 \\
    Kafka & apache/kafka:latest (KRaft, single broker) \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Dataset e parametri di training}

Il corpus è il bundle Kaggle \emph{ECG Arrhythmia Classification Dataset}~\cite{sakib2021kaggle,sakib2021edge}, che raccoglie in formato tabulare tre database distinti: MIT-BIH Arrhythmia Database~\cite{moody2001impact,goldberger2000physiobank}, MIT-BIH Supraventricular Arrhythmia Database e INCART 2-lead Arrhythmia Database.
L'esperimento ha utilizzato tutti e tre i file CSV presenti nel percorso configurato (\texttt{DATASET\_PATH}), per un totale di 712\,822 battiti dopo pulizia dei valori nulli (etichette \texttt{N}/\texttt{SVEB}/\texttt{VEB}/\texttt{F}/\texttt{Q}).
Lo split train/test è 80/20 con \texttt{seed = 42L} (\texttt{randomSplit}), per un training set di 570\,214 record e un test set di 142\,608 record.
Il classificatore è Random Forest con \texttt{numTrees=100}, \texttt{maxDepth=15}.

Per la valutazione clinica si fa riferimento allo standard AAMI EC57:2012~\cite{aami2012ec57}, che raggruppa le etichette beat-level in cinque super-classi: N (normali e bundle branch block), S (supraventricolari), V (ventricolari), F (fusion), Q (non classificabili).

Il protocollo distingue due livelli di valutazione: le metriche di classificazione sono calcolate sul test set hold-out del trainer batch (20\% del dataset, non visti durante il fit), mentre latenza e throughput sono misurati sul flusso streaming generato dal simulatore.
Questa separazione riflette il ciclo di vita reale di un sistema ML: il modello è validato offline prima del deploy, e le proprietà operative sono verificate in condizioni di serving con messaggi in arrivo da Kafka.

\subsection{Procedura di esecuzione}

\begin{enumerate}
  \item Avvio Kafka: \texttt{docker-compose up -d}.
  \item Configurazione di \texttt{ECG\_DATASET\_PATH} (o del fallback \texttt{DATASET\_PATH} in \texttt{Config.scala}).
  \item Esecuzione: \texttt{sbt run}, selezione \texttt{it.utiu.thesis.AppRunner}.
  \item Raccolta log da console Spark e ispezione directory Delta \texttt{spark-warehouse/ecg\_predictions}.
\end{enumerate}

\subsection{Criteri di successo}

Si considera soddisfacente il prototipo se: (i)~l'accuratezza sul test set supera il 90\%, coerente con la letteratura sul MIT--BIH; (ii)~la latenza end-to-end resta nell'ordine di alcuni secondi (5--11~s in regime stazionario), compatibile con scenari \emph{near-real-time} di screening ambulatoriale; (iii)~nessun messaggio viene perso dopo restart con checkpoint intatto; (iv)~le predizioni sono persistite su Delta in modo interrogabile con Spark SQL.
Questi criteri bilanciano qualità predittiva e proprietà non funzionali, evitando di valutare il sistema solo sull'accuratezza isolata.

\section{Performance del modello}\label{sec:performance-modello}

\subsection{Accuratezza globale}

Sul test set hold-out (142\,608 battiti, 20\% del corpus), il modello Random Forest ha raggiunto un'accuratezza globale del \textbf{98,16\%}, con precision pesata del 98,10\%, recall pesata del 98,16\% e F1-score pesato del 98,01\%.
Il training di 100 alberi con profondità massima 15 su 570\,214 campioni ha richiesto 127\,626~ms ($\approx$2\,min) su singolo nodo in modalità \texttt{local[*]}.
Il risultato si colloca nella fascia superiore degli studi su classificatori ensemble applicati al MIT-BIH~\cite{breiman2001random,moody2001impact}, dove accuratezze tra 95\% e 98\% sono frequenti con feature ingegnerizzate a livello di battito.

L'accuratezza globale aggrega performance su classi con supporto molto disomogeneo: la classe N domina il test set, e un errore su N ha peso numerico maggiore di un errore su F o Q.
Per questo motivo, le metriche per-classe della Tabella~\ref{tab:metriche-classe} e l'F1-score della Figura~\ref{fig:f1-per-classe} offrono una lettura più equilibrata, in linea con le raccomandazioni AAMI EC57~\cite{aami2012ec57} che richiedono valutazione separata per super-classe.

\subsection{Metriche per super-classe AAMI}

La Tabella~\ref{tab:metriche-classe} riporta precision, recall e F1-score per le super-classi AAMI.
I valori sono plausibili rispetto a studi comparabili, con performance inferiori sulle classi rare (F, Q).

\begin{table}[H]
  \centering
  \caption{Metriche per super-classe AAMI sul test set hold-out (run \texttt{20260616\_201739}).}
  \label{tab:metriche-classe}
  \begin{tabular}{@{}lcccc@{}}
    \toprule
    \textbf{Classe} & \textbf{Precision} & \textbf{Recall} & \textbf{F1} & \textbf{Support} \\
    \midrule
    N (normale)    & 0,984 & 0,998 & 0,991 & 128\,796 \\
    S (supravent.) & 0,911 & 0,618 & 0,737 & 3\,647 \\
    V (ventric.)   & 0,971 & 0,929 & 0,950 & 9\,854 \\
    F (fusion)     & 0,960 & 0,339 & 0,501 & 283 \\
    Q (unknown)    & 1,000 & 0,107 & 0,194 & 28 \\
    \midrule
    \textbf{Weighted avg} & 0,981 & 0,982 & 0,980 & 142\,608 \\
    \bottomrule
  \end{tabular}
\end{table}

\subsection{Matrice di confusione e analisi degli errori}

La Tabella~\ref{tab:confusion-matrix} riporta la matrice di confusione calcolata sul test set hold-out.
La Figura~\ref{fig:f1-per-classe} sintetizza l'F1-score per super-classe.
Gli errori principali si concentrano tra le classi S e N (1\,314 battiti SVEB classificati come N), fenomeno noto in letteratura per ectopie supraventricolari con morfologia QRS simile al ritmo sinusale.
Tra V e N si registrano 628 falsi negativi su 9\,854 battiti ventricolari; tra N e V vi sono 165 falsi positivi, un tasso contenuto data la dimensione della classe N.

\begin{table}[H]
  \centering
  \caption{Matrice di confusione sul test set hold-out (righe = veri, colonne = predetti).}
  \label{tab:confusion-matrix}
  \begin{tabular}{@{}lccccc@{}}
    \toprule
    \textbf{Reale $\backslash$ Predetto} & \textbf{N} & \textbf{V} & \textbf{S} & \textbf{F} & \textbf{Q} \\
    \midrule
    N (128\,796) & 128\,476 & 165 & 155 & 0 & 0 \\
    V (9\,854)   & 628      & 9\,158 & 64 & 4 & 0 \\
    S (3\,647)   & 1\,314   & 78  & 2\,255 & 0 & 0 \\
    F (283)      & 161      & 26  & 0   & 96 & 0 \\
    Q (28)       & 16       & 8   & 1   & 0  & 3 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[H]
  \centering
  \begin{tikzpicture}
    \begin{axis}[
      ybar,
      bar width=14pt,
      ymin=0, ymax=1,
      ylabel={F1-score},
      symbolic x coords={N, S, V, F, Q},
      xtick=data,
      nodes near coords,
      nodes near coords align={vertical},
      width=10cm,
      height=6cm,
      title={F1-score per super-classe AAMI}
    ]
      \addplot coordinates {(N,0.991) (S,0.737) (V,0.950) (F,0.501) (Q,0.194)};
    \end{axis}
  \end{tikzpicture}
  \caption{F1-score per super-classe AAMI sul test set hold-out (run \texttt{20260616\_201739}).}
  \label{fig:f1-per-classe}
\end{figure}

\subsection{Considerazioni sull'accuratezza}

L'accuratezza globale da sola è insufficiente in ambito clinico, come discusso nel Capitolo~\ref{ch:dominio-applicativo-e-dataset}.
Un modello che classifica tutto come ``normale'' otterrebbe alta accuratezza sul file MIT--BIH preprocessato, dove la classe \texttt{N} supera l'89\% dei battiti, ma fallirebbe sulle aritmie patologiche.
Per questo motivo l'analisi privilegia recall e F1 sulle classi V e S.

\subsection{Confronto con la letteratura}

Studi su feature beat-level simili a quelle del CSV Kaggle~\cite{dechazal2004automatic,sakib2021edge} riportano sensibilità e specificità per super-classe AAMI spesso superiori al 90\% per le classi N e V, con performance più variabili su S e F.
I risultati ottenuti si collocano nella fascia superiore di tale intervallo per N (F1 = 0,991) e V (F1 = 0,950), e nella media per S (F1 = 0,737).
Le classi F (F1 = 0,501) e Q (F1 = 0,194) evidenziano le difficoltà note in letteratura legate allo sbilanciamento estremo: nel test set F conta 283 esempi e Q soltanto 28.
Un miglioramento su queste classi richiederebbe strategie di ribilanciamento (SMOTE, class weighting) o validazione incrociata per soggetto (\emph{patient-wise split}), che evita leakage tra battiti della stessa registrazione.

\section{Analisi degli errori e impatto clinico}\label{sec:analisi-errori}

Le metriche numeriche della Sezione~\ref{sec:performance-modello} vanno lette insieme al contesto d'uso.
Un classificatore automatico di battiti non sostituisce il clinico: funge da \emph{supporto decisionale} che prioritizza eventi da rivedere, riducendo il tempo di screening su tracce lunghe.

\subsection{Scenari operativi e sensibilità richiesta}

\paragraph{Monitoraggio post-infarto in reparto.}
Dopo un evento ischemico, la ricorrenza di ectopie ventricolari (classe V) può anticipare aritmie più gravi.
Qui conta la \textbf{recall} sulla classe V: un falso negativo su una PVC isolata può essere accettabile, meno su raffiche ripetute non segnalate.
Con recall del 92,94\% sulla classe V, su 9\,854 battiti ventricolari nel test set il modello non rileva circa 695 ectopie ($\approx$7,1\%), pari a poco meno di una PVC su quattordici.

\paragraph{Holter ambulatoriale e telemetria domiciliare.}
Il paziente porta il dispositivo per 24--48 ore; il personale analizza offline migliaia di battiti.
In questo scenario, \textbf{falsi positivi} aumentano il carico di revisione e possono indurre abbandono del servizio da parte del paziente.
Una precision del 97,1\% sulla classe V implica che circa una predizione ``ventricolare'' su trentacinque è un falso positivo; un carico di revisione contenuto, ma non nullo, per sessioni Holter estese.

\paragraph{Pronto soccorso e triage.}
La latenza end-to-end della pipeline, nell'ordine di 5--11~s in regime stazionario (Tabella~\ref{tab:latenza}), è compatibile con un flusso \emph{near-real-time} per screening e prioritizzazione, ma il prototipo non integra workflow di alert verso il personale.
L'obiettivo sarebbe evidenziare subito tratti ad alto rischio (es.\ sequenze V consecutive, pause RR), non solo classificare battito per battito.

\subsection{Allineamento con AAMI EC57 e sbilanciamento del dataset}

Lo standard AAMI EC57:2012~\cite{aami2012ec57} definisce criteri di test per algoritmi di misura ritmica; in letteratura, le valutazioni sul MIT--BIH confrontano spesso sensibilità e specificità per super-classe.
Il nostro dataset preprocessato è fortemente sbilanciato verso \texttt{N} ($\approx$89,5\% dei battiti): la prevalenza clinica reale in popolazione generale non coincide con quella di un archivio cardiologico storico.
Un modello ottimizzato solo su questo CSV può \emph{sovrastimare} la performance su ritmo normale e \emph{sottostimare} la robustezza su classi rare (F, Q), proprio dove l'impatto clinico di un errore è meno prevedibile.

\subsection{Falsi negativi sulle ectopie ventricolari}

Oltre alle PVC, gli errori più significativi riguardano la classe S: con recall del 61,8\%, il modello non rileva circa 1\,392 ectopie supraventricolari su 3\,647, classificandole prevalentemente come N.
Il fenomeno riflette morfologie simili: la pipeline può classificare come normale un battito supraventricolare con QRS stretto.
In terapia intensiva, ectopie supraventricolari ripetute possono richiedere diversa gestione farmacologica rispetto al ritmo sinusale.

Strategie non implementate nel prototipo ma rilevanti in un'evoluzione clinica:
\begin{itemize}
  \item \textbf{Cost-sensitive learning}: penalizzare maggiormente errori su classi V e S durante il training.
  \item \textbf{Soglie operative}: abbassare la soglia di decisione per la classe V accettando più falsi positivi.
  \item \textbf{Regole cliniche ibride}: combinare output ML con vincoli su intervalli RR (es.\ pausa $>$ 2~s) o consecutività di ectopie.
\end{itemize}

\subsection{Falsi positivi e carico operativo}

Il trade-off precision/recall non è universale: la telemetria domiciliare privilegia la specificità (meno allarmi notturni inutili), mentre in UTI può prevalere la sensibilità.
Il prototipo non espone soglie configurabili per classe: usa l'argmax implicito del Random Forest, lasciando al deployment futuro la calibrazione per scenario.

\subsection{Prioritizzazione degli alert}

In un sistema operativo, non tutti gli errori di classificazione hanno la stessa urgenza.
Un falso negativo su una singola PVC isolata in un Holter ambulatoriale ha impatto diverso da un falso negativo su una raffica di ectopie ventricolari in un paziente post-infarto.
La pipeline attuale classifica battito per battito senza aggregare eventi in finestre temporali; un'estensione naturale consisterebbe in una fase di post-processing che raggruppa predizioni consecutive (es.\ tre V in 10 secondi) prima di generare un alert, riducendo il rumore e allineando l'output al ragionamento clinico.

\section{Analisi della latenza}\label{sec:analisi-latenza}

\subsection{Definizione end-to-end}

Si definisce latenza end-to-end il tempo intercorrente tra la generazione del messaggio da \texttt{EcgPatientSimulator} (\texttt{producer.send}) e la visibilità della predizione nella tabella Delta Lake.
Questa misura include tutte le fasi intermedie: serializzazione e invio al broker, permanenza nel buffer Kafka, scheduling del micro-batch Spark, parsing del payload JSON, applicazione del modello ML e scrittura transazionale su Delta.
Non include il tempo di acquisizione del segnale fisico né il preprocessing edge, poiché il simulatore opera su dati già estratti dal CSV.
La definizione è conservativa: la visibilità su Delta coincide con il commit del micro-batch, non con il momento in cui il dato è interrogabile da un eventuale dashboard downstream.

\subsection{Latenza misurata}

La Tabella~\ref{tab:latenza} riporta le metriche di latenza misurate sull'esperimento definitivo (\texttt{20260616\_201739}), escludendo il primo mega-batch (batch~0) generato dall'accumulo di messaggi precedenti durante il warmup dello Spark context.
In regime stazionario (batch 1--133, 1000 record inviati dal simulatore a 1~msg/s) la latenza end-to-end per singolo record oscilla tra 4\,981 e 10\,948~ms, con una media per-batch di circa 8\,500~ms.

\begin{table}[H]
  \centering
  \caption{Latenza end-to-end misurata in regime stazionario (run \texttt{20260616\_201739}).}
  \label{tab:latenza}
  \begin{tabular}{@{}lr@{}}
    \toprule
    \textbf{Metrica} & \textbf{Valore} \\
    \midrule
    Latenza minima per record & 4\,981~ms \\
    Latenza media per micro-batch & $\approx$8\,500~ms \\
    Latenza massima per record & 10\,948~ms \\
    Durata media micro-batch & $\approx$6\,500~ms \\
    Throughput osservato & 1,93~msg/s \\
    Throughput configurato (simulatore) & 1,0~msg/s \\
    \bottomrule
  \end{tabular}
\end{table}

Il collo di bottiglia principale è la fase di scheduling e processing del micro-batch Spark, che include serializzazione, parsing JSON, inferenza RF, scrittura Delta e il \texttt{show()} di debug nel \texttt{foreachBatch}.
Con trigger di default (\emph{as-soon-as-possible}) e singolo worker in modalità \texttt{local[*]}, la JVM esegue sequenzialmente le fasi precedenti; la latenza risultante è dell'ordine di alcuni secondi, non sub-secondo.

\subsection{Warmup iniziale}

L'attesa di 15 secondi in \texttt{AppRunner} prima dell'avvio del simulatore introduce un offset fisso non incluso nella latenza per-record.
Al primo avvio, Spark Streaming impiega tempo aggiuntivo per inizializzare il query plan, caricare il modello da disco e stabilire la connessione con il broker Kafka.
Nei run successivi, con checkpoint esistente, il warmup si riduce perché Spark può saltare la negoziazione iniziale degli offset e ripartire dal punto di arresto precedente.
La durata di 15 secondi è stata determinata empiricamente sulla macchina di sviluppo; su hardware differente o con JVM non ancora warm, potrebbe non essere sufficiente, causando la perdita dei primi messaggi nel buffer Kafka prima che il consumer sia pronto a elaborarli.

\subsection{Confronto con requisiti clinici}

Per monitoraggio in reparto o in terapia intensiva, latenze sotto i 500~ms sono spesso citate come target per allarmi critici.
I valori misurati in Tabella~\ref{tab:latenza} (5--11~s) superano tale soglia: la pipeline è adeguata a screening e prioritizzazione \emph{offline-near-real-time}, non a controllo closed-loop su singolo battito.
In scenari ambulatoriali o di Holter 24~h, dove le predizioni vengono aggregate e revisionate dal clinico in un secondo momento, una latenza dell'ordine del secondo è pienamente accettabile.
Per latenze inferiori a 1~s si richiederebbe un trigger fisso sub-secondo in Structured Streaming e un modello ottimizzato per inferenza a bassa latenza (es.\ albero singolo o rete neurale leggera quantizzata), modifiche che esulano dall'obiettivo architetturale di questa tesi.

\section{Throughput e back-pressure}\label{sec:throughput}

\subsection{Misura in condizioni nominali}

Con rate di ingresso pari a 1~msg/s (limite imposto dal simulatore con \texttt{Thread.sleep(1000)}), il predictor processa ogni micro-batch senza accumulo di backlog, come confermato dall'assenza di lag crescente negli offset Kafka.
Il throughput osservato di 1,93~msg/s riflette l'elaborazione in coda del primo mega-batch, non un picco in regime stazionario: in condizioni normali il sistema segue il rate configurato del producer.
Il throughput massimo sostenibile non è stato misurato direttamente; test di stress con rate crescenti (10, 100~msg/s) e rimozione del \texttt{Thread.sleep} nel simulatore costituiscono un'estensione naturale della valutazione.

\subsection{Effetto delle partizioni Kafka}

Con topic a partizione singola (default Docker), il parallelismo di consumo Spark è limitato a un task per micro-batch: ogni micro-batch legge l'intera partizione con un singolo thread.
Aumentare le partizioni del topic e il parametro \texttt{spark.default.parallelism} permetterebbe scaling orizzontale del consumer, come discusso nella Sezione~\ref{sec:requisiti-nf}.
Se il topic avesse, ad esempio, quattro partizioni con chiave paziente, Spark allocherebbe fino a quattro task paralleli per micro-batch, distribuendo il carico di parsing e inferenza su altrettanti core.
Il throughput crescerebbe in modo approssimativamente lineare fino a saturare le risorse di CPU o I/O, a patto che il modello ML e lo schema JSON restino invariati tra le partizioni.

\subsection{Back-pressure}

Structured Streaming applica back-pressure nativo: se il consumer non riesce a tenere il passo con il producer, i lag di offset Kafka crescono e il motore rallenta l'avvio di nuovi micro-batch fino al recupero.
Nel setup attuale, con 1 msg/s, il lag resta nullo perché il predictor completa ogni micro-batch ben prima dell'arrivo del messaggio successivo.
Con rate superiori, il lag di offset diventerebbe l'indicatore principale di saturazione: un lag crescente segnala che il tempo di processing per micro-batch supera l'intervallo di arrivo dei messaggi.
Test di stress con rate superiori (10, 100, 1000 msg/s) costituirebbero un'estensione naturale della valutazione, permettendo di individuare il punto di saturazione del predictor su singolo nodo e di stimare il numero di executor necessari per carichi realistici.

\subsection{Stima del carico multi-paziente}

Un singolo paziente con frequenza cardiaca media di 70 bpm genera circa 1,2 battiti al secondo.
Cento pazienti monitorati simultaneamente implicherebbero $\sim$120~msg/s; raggiungere tale carico richiederebbe più partizioni Kafka e un trigger Spark con parallelismo adeguato.
Mille pazienti ($\sim$1200~msg/s) richiederebbero quasi certamente un cluster Spark con più executor e un topic Kafka con decine di partizioni, con Delta su object store per lo storage scalabile delle predizioni.
Questi ordini di grandezza illustrano come il prototipo a 1 msg/s sia rappresentativo dell'architettura ma non del carico operativo di un servizio e-Health su larga scala.

\section{Scalabilità}\label{sec:scalabilita}

\subsection{Limiti del benchmark mono-nodo}

Tutte le misure precedenti si riferiscono a un deployment su singola macchina.
Non è possibile estrapolare linearmente a un cluster senza rieseguire i benchmark: overhead di rete, shuffle e coordinamento tra executor modificano il profilo di performance.

\subsection{Proiezione su cluster}

In un deployment distribuito, i componenti scalerebbero in modo indipendente:
\begin{itemize}
  \item \textbf{Kafka}: aggiunta broker e partizioni per throughput di ingresso;
  \item \textbf{Spark Streaming}: più executor per parallelismo di consumo;
  \item \textbf{Delta Lake}: object store con scritture concorrenti gestite dal transaction log.
\end{itemize}

Il trainer batch beneficerebbe di più core e memoria per il fit del Random Forest su dataset completi (non limitati a CSV preprocessati).
Il simulatore andrebbe sostituito da producer reali o da tool di load testing (Kafka \texttt{kafka-producer-perf-test}).

\subsection{Costi di scaling}

L'aggiunta di executor Spark e broker Kafka comporta costi infrastrutturali (CPU, memoria, storage, banda) che crescono sub-linearmente se il partitioning è calibrato correttamente.
Delta su object store scala lo storage in modo elastico, ma le scritture frequenti di micro-batch piccoli possono generare molti file Parquet di dimensione ridotta, richiedendo periodicamente \texttt{OPTIMIZE} per mantenere prestazioni di query accettabili.
Questi aspetti operativi, trascurati nel prototipo, diventano centrali in un deployment con migliaia di pazienti e retention di predizioni su mesi o anni.

\section{Riproducibilità}\label{sec:riproducibilita}

Per replicare l'esperimento:

\begin{enumerate}
  \item Scaricare il bundle da Kaggle~\cite{sakib2021kaggle} (\url{https://www.kaggle.com/datasets/sadmansakib7/ecg-arrhythmia-classification-dataset}) e collocare i tre file CSV utilizzati nell'esperimento (MIT--BIH Arrhythmia Database, MIT--BIH Supraventricular Arrhythmia Database e INCART 2-lead Arrhythmia Database) nella directory puntata da \texttt{ECG\_DATASET\_PATH} o dal fallback \texttt{DATASET\_PATH} in \texttt{Config.scala}.
  \item Configurare \texttt{ECG\_DATASET\_PATH} (o \texttt{DATASET\_PATH} in \texttt{Config.scala}).
  \item Avviare Kafka con \texttt{docker-compose up -d}.
  \item Eseguire \texttt{sbt run} e selezionare \texttt{AppRunner}.
  \item Ispezionare output in \texttt{ml-model/}, \texttt{spark-warehouse/ecg\_predictions} e log console.
\end{enumerate}

Lo \texttt{seed = 42L} garantisce riproducibilità dello split train/test.
La riproducibilità end-to-end del flusso streaming dipende anche dallo stato del checkpoint: per riesperimenti puliti, cancellare \texttt{spark-warehouse/checkpoints/ecg\_predictions} prima di ogni run.

\section{Sintesi dei risultati sperimentali}

I risultati dell'esperimento \texttt{20260616\_201739} confermano che la pipeline raggiunge e supera gli obiettivi prefissati per quanto riguarda la qualità predittiva:
accuratezza globale del 98,16\%, F1-score pesato del 98,01\%, con performance solide sulle classi N e V.
Le classi rare F e Q restano il punto debole, coerentemente con il loro supporto estremamente ridotto nel dataset (283 e 28 campioni nel test set rispettivamente).
La latenza end-to-end si assesta nell'ordine di 5--11~s in regime stazionario, valore superiore alla soglia sub-secondo inizialmente indicata come target ma compatibile con scenari di screening e monitoraggio ambulatoriale.

\subsection{Limiti della valutazione sperimentale}

La valutazione presentata copre il classificatore batch sul test set hold-out e le proprietà non funzionali della pipeline streaming con carico di 1~msg/s.
Non è stata eseguita validazione incrociata per soggetto (split per colonna \texttt{record}), che eviterebbe che battiti della stessa registrazione compaiano sia in training che in test; questa limitazione può gonfiare leggermente le metriche sulle classi più frequenti.
Non sono stati confrontati modelli alternativi (GBT, SVM) sullo stesso split, né è stata misurata la latenza a livello di singola fase con strumenti di profiling dedicati.
La latenza end-to-end include anche operazioni di debug (\texttt{batchDF.show()}) eseguite nel \texttt{foreachBatch}, che in produzione verrebbero rimosse, abbassando sensibilmente il processing time per micro-batch.
Questi limiti sono coerenti con l'obiettivo della tesi --- dimostrare l'integrazione end-to-end --- ma delimitano il campo di applicabilità delle conclusioni numeriche.
