\section{Pattern architetturali: Lambda, Kappa e posizionamento del progetto}\label{sec:lambda-kappa}

Le architetture per l'elaborazione di dati in tempo reale si sono consolidate attorno a due modelli di riferimento~\cite{marz2015big,kreps2014questioning,kleppmann2017designing}.

L'\textbf{architettura Lambda} prevede due pipeline parallele: un \emph{batch layer} che calcola viste corrette su dati storici e uno \emph{speed layer} che gestisce gli aggiornamenti recenti con latenza ridotta; un \emph{serving layer} unifica i risultati per le query.
La forza della Lambda è la correttezza batch su volumi elevati; il costo è la duplicazione della logica (stesse aggregazioni implementate due volte) e la complessità operativa.

L'\textbf{architettura Kappa} propone un unico flusso di eventi: lo speed layer consuma il log; il riprocessamento storico si ottiene rileggendo il topic dall'inizio con job batch-equivalenti.
Si elimina la duplicazione logica, ma servono formati immutabili, retention adeguata e capacità di ricalcolo su scala.
La Figura~\ref{fig:lambda-schema} e la Tabella~\ref{tab:lambda-kappa-confronto} riassumono la Lambda e il confronto con l'impostazione adottata.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    node distance=0.85cm and 2.0cm,
    layer/.style={rectangle, draw, rounded corners, minimum width=2.6cm, minimum height=0.85cm, align=center, font=\small},
    store/.style={cylinder, draw, shape border rotate=90, aspect=0.22, minimum height=1.0cm, minimum width=1.6cm, align=center, font=\footnotesize},
    arrow/.style={-{Stealth[length=2mm]}, thick}
  ]
    \node[store] (ingest) {Event log};
    \node[layer, above left=1.2cm and 0.1cm of ingest] (batch) {Batch layer};
    \node[layer, above right=1.2cm and 0.1cm of ingest] (speed) {Speed layer};
    \node[layer, above=2.6cm of ingest] (serve) {Serving layer};

    \draw[arrow] (ingest.north) -- ++(0,0.5) -| (batch.south);
    \draw[arrow] (ingest.north) -- ++(0,0.5) -| (speed.south);
    \draw[arrow] (batch.north) -- (serve.south);
    \draw[arrow] (speed.north) -- (serve.south);

    \node[font=\footnotesize, below=0.35cm of ingest] {Vista Lambda};
  \end{tikzpicture}
  \caption{Schema concettuale dell'architettura Lambda: batch e speed layer alimentano il serving.}
  \label{fig:lambda-schema}
\end{figure}

Il progetto di questa tesi adotta un \textbf{ibrido pragmatico}.
L'addestramento avviene in batch off-line (\texttt{EcgModelTrainer}); l'inferenza opera in streaming (\texttt{EcgStreamingPredictor}).
Non esiste uno speed layer che ricalcoli aggregati su finestra mobile: il modello è un artefatto statico ricaricato dal filesystem fino a un nuovo training.
La configurazione è vicina alla Lambda con confine netto training/serving, pratica comune nei sistemi ML operativi dove la frequenza di retraining è ordini di grandezza inferiore a quella di inferenza~\cite{sculley2015hidden}.

\begin{table}[H]
  \centering
  \caption{Confronto sintetico Lambda, Kappa e impostazione del progetto.}
  \label{tab:lambda-kappa-confronto}
  \footnotesize
  \begin{tabular}{@{}p{2.4cm}p{2.9cm}p{2.9cm}p{3.3cm}@{}}
    \toprule
    \textbf{Aspetto} & \textbf{Lambda} & \textbf{Kappa} & \textbf{Questo progetto} \\
    \midrule
    Logica di calcolo & Duplicata (batch + speed) & Unificata sul log & Train vs infer separati \\
    Latenza inferenza & Bassa (speed layer) & Bassa & Bassa (streaming) \\
    Correttezza storica & Alta (batch) & Ricalcolo dal log & Training batch su CSV \\
    Complessità ops & Alta & Media & Bassa (prototipo) \\
    Model serving & Non specificato & Non specificato & Artefatto ML su FS \\
    \bottomrule
  \end{tabular}
\end{table}

Rispetto alla Kappa pura, manca la possibilità di riaddestrare rileggendo soltanto Kafka: il training legge CSV storici, non il topic.
Rispetto alla Lambda pura, manca un serving layer che fondo viste batch e real-time; le predizioni sono scritte direttamente su Delta Lake dal predictor.

La letteratura sui sistemi ML in produzione~\cite{sculley2015hidden} evidenzia come la separazione tra training e serving sia una delle fonti principali di \emph{technical debt}: pipeline di feature engineering diverse, versioni di librerie non allineate, assenza di monitoraggio del drift.
Il progetto mitiga parte di questo rischio usando la stessa \texttt{PipelineModel} serializzata per batch e streaming, e lo stesso schema JSON derivato dal CSV di training.
Restano tuttavia gap noti: nessun registry versionato del modello, nessun monitoraggio automatico della distribuzione delle feature in ingresso, nessun meccanismo di rollback se un nuovo modello degrada le metriche.

\section{Panoramica della pipeline}\label{sec:panoramica-pipeline}

La pipeline si articola in tre moduli funzionali e un orchestratore:

\begin{enumerate}
  \item \textbf{Batch layer} --- \texttt{EcgModelTrainer}: legge CSV storici, addestra la pipeline ML, salva il modello su filesystem.
  \item \textbf{Ingestion layer} --- \texttt{EcgPatientSimulator}: simula un dispositivo edge, pubblica battiti JSON su Kafka.
  \item \textbf{Streaming layer} --- \texttt{EcgStreamingPredictor}: consuma Kafka, classifica, persiste su Delta Lake.
  \item \textbf{Orchestratore} --- \texttt{AppRunner}: coordina l'avvio sequenziale dei componenti.
\end{enumerate}

La Figura~\ref{fig:architettura-logica} mostra il flusso logico dei dati; la Figura~\ref{fig:sequenza-avvio} ne dettaglia la sequenza temporale di avvio.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    block/.style={rectangle, draw, rounded corners, minimum width=2.5cm, minimum height=1.05cm, align=center, font=\small, inner sep=4pt},
    store/.style={cylinder, draw, shape border rotate=90, aspect=0.25, minimum height=1.15cm, minimum width=1.85cm, align=center, font=\small, inner sep=2pt},
    arrow/.style={-{Stealth[length=2mm]}, thick},
    orch/.style={dashed, gray, thick}
  ]
    \coordinate (colA) at (0,0);
    \coordinate (colB) at (4.3,0);
    \coordinate (colC) at (8.6,0);
    \coordinate (colD) at (12.9,0);

    \node[store] (csv) at (colA) {CSV\\MIT-BIH};
    \node[block] (trainer) at (colB) {\texttt{EcgModel}\\[-1pt]\texttt{Trainer}};
    \node[store] (model) at (colC) {PipelineModel\\su FS};

    \node[block] (sim) at ($(colA)+(0,-2.85)$) {\texttt{EcgPatient}\\[-1pt]\texttt{Simulator}};
    \node[block] (kafka) at ($(colB)+(0,-2.85)$) {Kafka\\{\scriptsize ecg-input-stream}};
    \node[block] (predictor) at ($(colC)+(0,-2.85)$) {\texttt{EcgStreaming}\\[-1pt]\texttt{Predictor}};
    \node[store] (delta) at ($(colD)+(0,-2.85)$) {Delta Lake\\predictions};

    \node[block] (runner) at ($(colB)+(0,-1.42)$) {\texttt{AppRunner}};

    \draw[arrow] (csv) -- (trainer);
    \draw[arrow] (trainer) -- (model);
    \draw[arrow] (csv) -- (sim);
    \draw[arrow] (sim) -- (kafka);
    \draw[arrow] (kafka) -- (predictor);
    \draw[arrow] (model) -- (predictor);
    \draw[arrow] (predictor) -- (delta);

    \draw[orch] (runner.north) -- (trainer.south);
    \draw[orch] (runner.south) -- (kafka.north);
    \draw[orch] (runner.south) -- (sim.north east);
    \draw[orch] (runner.south) -- (predictor.north west);
  \end{tikzpicture}
  \caption{Flusso logico: training batch, ingestion simulata e inferenza streaming. Linee tratteggiate: orchestrazione \texttt{AppRunner}.}
  \label{fig:architettura-logica}
\end{figure}

\subsection{Flusso granulare dei dati}

In sintesi: il trainer costruisce il modello da CSV; il simulatore pubblica JSON su Kafka; il predictor deserializza, classifica e appende su Delta con checkpoint.
Il dettaglio implementativo (listing, \texttt{foreachBatch}, configurazione) è nel Capitolo~\ref{ch:implementazione-e-dettagli-tecnici}.
Il modello ML (\texttt{PipelineModel.load}) è l'unico artefatto condiviso tra batch e streaming.

\paragraph{Formato del messaggio Kafka.}
Ogni record pubblicato dal simulatore è una stringa JSON che serializza una riga del CSV: identificativo \texttt{record}, etichetta \texttt{type} (ground truth disponibile per valutazione, ma non usata in inferenza), e le 32 feature numeriche con prefissi \texttt{0\_} e \texttt{1\_}.
Il consumer Spark non riceve metadati Kafka aggiuntivi oltre a \texttt{key}, \texttt{value}, \texttt{timestamp} e \texttt{partition}; la logica applicativa opera esclusivamente sul campo \texttt{value} parsato.
In un deployment reale, si potrebbe arricchire il payload con timestamp di acquisizione sul dispositivo, identificativo paziente pseudonimizzato e versione del firmware di estrazione feature, campi assenti nel prototipo ma utili per tracciabilità e debug.

\paragraph{Persistenza delle predizioni.}
Ogni micro-batch processato produce righe Delta con colonne \texttt{record}, \texttt{true\_label}, \texttt{prediction} e \texttt{probability}.
La colonna \texttt{true\_label} deriva dal campo \texttt{type} del messaggio JSON e consente valutazione ex post confrontando predizione e etichetta reale, senza richiedere un join con il dataset di training.
La colonna \texttt{probability} contiene il vettore di probabilità per classe, utile per analisi di calibrazione o per soglie operative diverse dall'argmax implicito del classificatore.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    node distance=0.35cm,
    actor/.style={rectangle, draw, minimum width=2.1cm, minimum height=0.7cm, font=\footnotesize},
    lifeline/.style={dashed, gray},
    msg/.style={-{Stealth[length=1.5mm]}, font=\tiny}
  ]
    \node[actor] (ar) at (0,0) {AppRunner};
    \node[actor] (tr) at (3.2,0) {Trainer};
    \node[actor] (pr) at (6.4,0) {Predictor};
    \node[actor] (si) at (9.6,0) {Simulator};
    \foreach \x in {0,3.2,6.4,9.6} {
      \draw[lifeline] (\x,-0.4) -- (\x,-5.8);
    }
    \draw[msg] (0,-0.95) -- node[above] {train if missing} (3.2,-0.95);
    \draw[msg] (0,-1.85) -- node[above] {start thread} (6.4,-1.85);
    \node[font=\tiny, align=center] at (6.4,-2.65) {sleep 15s\\warmup};
    \draw[msg] (0,-3.35) -- node[above] {main} (9.6,-3.35);
    \draw[msg] (9.6,-4.35) -- node[above] {1000 msg} (6.4,-4.35);
    \draw[msg] (0,-5.35) -- node[above] {join} (6.4,-5.35);
  \end{tikzpicture}
  \caption{Sequenza di avvio orchestrata da \texttt{AppRunner}: training condizionale, predictor in thread demone, attesa, simulatore, join.}
  \label{fig:sequenza-avvio}
\end{figure}

\section{Vista fisica e deployment}\label{sec:vista-fisica}

In ambiente sperimentale, l'intera pipeline gira su un singolo host di sviluppo.
La Figura~\ref{fig:deployment} illustra la disposizione fisica.

\begin{figure}[H]
  \centering
  \begin{tikzpicture}[
    block/.style={rectangle, draw, rounded corners, minimum width=2.5cm,
                  minimum height=0.95cm, align=center, font=\small, inner sep=4pt},
    docker/.style={block, fill=gray!8},
    arrow/.style={-{Stealth[length=2mm]}, thick},
    lbl/.style={font=\tiny, fill=white, inner sep=1.5pt}
  ]
    \node[block]  (trainer)   at (-3.6, 0) {JVM Spark\\Trainer};
    \node[block]  (predictor) at ( 0.0, 0) {JVM Spark\\Predictor};
    \node[block]  (simulator) at ( 3.6, 0) {JVM Spark\\Simulator};

    \node[block]  (fs)    at (-3.6, -2.6) {FS locale\\model + delta};
    \node[docker] (kafka) at ( 3.6, -2.6) {Docker Kafka\\KRaft :9092};

    \node[rectangle, draw, dashed, rounded corners,
          fit={(trainer) (simulator) (fs) (kafka)},
          inner sep=20pt,
          label={[font=\small\bfseries]above:Host di sviluppo (localhost)}] {};

    \draw[arrow] (trainer.south) -- node[left, lbl] {write} (fs.north);
    \draw[arrow] (predictor.south west) -- node[left, lbl] {read/write} (fs.north east);
    \draw[arrow] (simulator.south) -- node[right, lbl] {produce} (kafka.north);
    \draw[arrow] (kafka.north west) -- node[right, lbl] {consume} (predictor.south east);
  \end{tikzpicture}
  \caption{Deployment mono-nodo: tre processi JVM Spark (\texttt{local[*]}) e un container Docker per Kafka.}
  \label{fig:deployment}
\end{figure}

\subsection{Limiti del setup mono-nodo}

\texttt{SPARK\_MASTER = "local[*]"} impiega tutti i core su un'unica macchina; Kafka ha replication factor 1.
Un crash del host interrompe l'intera pipeline.
In produzione: cluster Spark dedicato, Kafka multi-broker con RF$\geq$3, Delta su object store (S3, ADLS).
Il setup attuale è deliberatamente minimalista per dimostrare l'integrazione end-to-end.

Un deployment distribuito tipico separerebbe i ruoli in modo più netto: il trainer girerebbe su un cluster Spark dedicato o su un servizio schedulato (Databricks, EMR); il predictor su un cluster streaming con executor sempre attivi; Kafka su un cluster multi-broker con replication factor $\geq 3$; Delta su object store (S3, Azure Data Lake Storage) con accesso da più job.
Il filesystem locale usato nel prototipo per modello e predizioni è un sostituto didattico dello storage condiviso, ma non offre durabilità né condivisione tra nodi.
La Figura~\ref{fig:deployment} va letta come vista \emph{as-is} del laboratorio, non come target di produzione.

\section{Fase di addestramento: \texttt{EcgModelTrainer}}\label{sec:ecg-model-trainer}

Il modulo batch implementa il ciclo completo di machine learning supervisionato.

\subsection{Flusso operativo}

\begin{enumerate}
  \item Lettura CSV da \texttt{DATASET\_PATH} con header e inferenza schema.
  \item Pulizia: \texttt{na.drop()} elimina righe con valori mancanti.
  \item Split train/test 80/20 con \texttt{seed = 42L} per riproducibilità.
  \item Costruzione pipeline: \texttt{StringIndexer} $\rightarrow$ \texttt{VectorAssembler} $\rightarrow$ \texttt{RandomForestClassifier}.
  \item Addestramento (\texttt{pipeline.fit}), valutazione su test set, salvataggio su \texttt{ML\_MODEL\_PATH}.
\end{enumerate}

\subsection{Scelta del classificatore e alternative}

Random Forest è configurato con \texttt{numTrees=100} e \texttt{maxDepth=15}~\cite{breiman2001random}.
La scelta risponde alla natura tabellare delle feature (intervalli \texttt{pre-RR}/\texttt{post-RR}, morfologia QRS e intervalli PQ/QT/ST su due derivazioni) e alla capacità degli ensemble di catturare interazioni non lineari senza normalizzazione rigorosa.

Alternative valutabili nello stesso stack Spark includono:
\begin{itemize}
  \item \textbf{GBTClassifier} (gradient boosting nativo MLlib): spesso competitivo su dati tabellari, con tempi di training superiori e maggiore rischio di overfitting senza tuning;
  \item \textbf{LinearSVC / LogisticRegression}: baseline interpretabili, meno adatte se le interazioni tra feature RR e morfologia QRS sono forti;
  \item \textbf{Reti neurali}: richiederebbero export e serving fuori dalla Pipeline MLlib, aumentando la complessità operativa senza beneficio dimostrato su questo dataset nel prototipo.
\end{itemize}

\textbf{Feature importance.} Random Forest fornisce, per ogni albero, una misura di impurezza ridotta per split; aggregando sui 100 alberi si ottiene un ranking delle feature utile per dialogare con il dominio clinico (quali descrittori RR o QRS guidano le confusions tra N e V).
Non è implementata nel codice attuale, ma resta un'estensione naturale per migliorare l'interpretabilità senza cambiare architettura.

\textbf{Cross-validation.} MLlib espone \texttt{CrossValidator} e \texttt{TrainValidationSplit} per grid search su \texttt{numTrees} e \texttt{maxDepth}.
Non sono stati usati: il focus della tesi è l'integrazione streaming, e il dataset preprocessato ha dimensione moderata; un tuning sistematico migliorerebbe la generalizzazione a costo di tempi di sperimentazione molto più lunghi.

Il flusso di training può essere eseguito indipendentemente dal resto della pipeline.
Se il modello esiste già in \texttt{ML\_MODEL\_PATH}, \texttt{AppRunner} salta la fase di addestramento e procede direttamente all'inferenza.
Questa logica condizionale consente di iterare rapidamente sul predictor e sul simulatore senza ripetere il fit del Random Forest, riducendo i tempi di sviluppo.
In produzione, il training sarebbe schedulato (notturno, settimanale) e il predictor caricherebbe sempre l'ultima versione validata dal registry, non un path statico sul filesystem locale.

\subsection{Output del trainer}

Il modello salvato include metadata, stages serializzati e statistiche dell'indicizzatore.
In streaming, etichette mai viste in training sono gestite da \texttt{setHandleInvalid("keep")}, che assegna un indice dedicato anziché sollevare eccezioni: robustezza operativa a scapito di predizioni potenzialmente poco informative su classi \emph{unknown}.

\section{Simulazione IoT: \texttt{EcgPatientSimulator}}\label{sec:ecg-patient-simulator}

Il simulatore riproduce un dispositivo wearable che trasmette battiti verso un broker centralizzato.

\subsection{Logica di produzione}

\begin{enumerate}
  \item Lettura di al massimo 1000 record dal CSV (\texttt{.limit(1000)}).
  \item Serializzazione JSON tramite \texttt{to\_json(struct(col("*")))}.
  \item Raccolta in memoria (\texttt{.collect()}) e invio sequenziale con \texttt{KafkaProducer}.
  \item Intervallo di un secondo tra messaggi (\texttt{Thread.sleep(1000)}).
\end{enumerate}

L'uso di \texttt{KafkaProducer} Java puro, anziché \texttt{df.writeStream.format("kafka")}, avvicina il simulatore a un producer esterno a Spark, come avverrebbe su firmware edge senza motore distribuito.

\subsection{Schema del payload e impatto sulla banda}

Ogni messaggio serializza \emph{tutte} le colonne del CSV: identificativo \texttt{record}, etichetta \texttt{type}, feature numeriche e metadati.
La chiave \texttt{beat-\{index\}} serve al debug ma non è sfruttata per partizionamento semantico (paziente, sessione).
In produzione, si potrebbe ridurre il payload inviando solo le feature richieste dal modello, con risparmio di banda e minor superficie di esposizione dati; il contratto andrebbe però versionato esplicitamente (Schema Registry, vedi \S\ref{subsec:schema-registry}).

\subsection{Estensioni e limiti}

Il rate fisso di 1~msg/s è una semplificazione rispetto a un Holter reale (jitter, burst dopo riconnessione, gap di trasmissione).
Estensioni naturali: intervallo casuale gaussiano intorno alla frequenza cardiaca; simulazione di disconnessioni; ripresa con \texttt{startingOffsets} e idempotenza lato consumer.

\texttt{.collect()} materializza 1000 record sul driver: accettabile in demo, inaccettabile per flussi continui.
Questi limiti sono consapevoli e discussi nelle conclusioni.

Il simulatore legge dal CSV le prime 1000 righe nell'ordine in cui compaiono nel file, senza mescolamento casuale né campionamento stratificato per classe.
Ciò implica che la sequenza di messaggi Kafka riflette la distribuzione locale del file, non necessariamente quella globale del dataset.
Per test di stress o per valutare il comportamento su classi rare, si potrebbe filtrare il CSV prima della pubblicazione o aumentare il limite, accettando tempi di esecuzione più lunghi proporzionali al rate di 1 msg/s.

\section{Inferenza in tempo reale: \texttt{EcgStreamingPredictor}}\label{sec:ecg-streaming-predictor}

Il predictor è il cuore operativo della pipeline in fase di serving.

\subsection{Bootstrap dello schema JSON}

Anziché hardcodare uno \texttt{StructType}, il codice legge lo schema dal CSV di training:

\begin{lstlisting}[language=Scala,caption={Inferenza schema da CSV per \texttt{from\_json}.},label={lst:schema-bootstrap}]
val jsonSchema = spark.read
  .option("header", "true")
  .option("inferSchema", "true")
  .csv(datasetPath)
  .schema
\end{lstlisting}

Vantaggio: nessuna duplicazione manuale tra producer e consumer.
Svantaggio: accoppiamento implicito al layout del CSV; una modifica delle colonne richiede riavvio coerente di simulatore e predictor.

\subsection{Schema Registry e contratto dati}\label{subsec:schema-registry}

In ambienti industriali, \textbf{Confluent Schema Registry} (o equivalenti) gestisce schemi Avro, JSON Schema o Protobuf con regole di compatibilità (\emph{backward}, \emph{forward}).
Il producer registra la versione $v$ del messaggio; il consumer la risolve a runtime, fallendo in modo controllato se il contratto non è compatibile.
Per il prototipo accademico, l'inferenza da CSV è sufficiente; per produzione clinica, un contratto esplicito riduce il rischio di parsing silenzioso con campi mancanti o tipi errati dopo aggiornamento firmware del dispositivo.

\subsection{Pipeline di inferenza}

\begin{enumerate}
  \item \texttt{readStream.format("kafka")} con \texttt{startingOffsets=earliest} (solo al primo avvio senza checkpoint).
  \item Cast del payload a stringa, parsing con \texttt{from\_json}.
  \item Trasformazione tramite \texttt{pipelineModel.transform}.
  \item Proiezione su \texttt{record}, \texttt{true\_label}, \texttt{prediction}, \texttt{probability}.
  \item Scrittura su Delta Lake via \texttt{foreachBatch} con checkpoint.
\end{enumerate}

\subsection{Ciclo di vita del modello in produzione}

Il pattern attuale --- modello su directory locale, caricato all'avvio del predictor --- è tipico dei prototipi ma insufficiente per operazioni mature~\cite{sculley2015hidden}:
\begin{itemize}
  \item \textbf{Model serving}: registry (MLflow, servizio REST) con versioni immutabili e metadati di training;
  \item \textbf{A/B testing}: routing di una frazione del traffico su modello candidato;
  \item \textbf{Model drift}: monitoraggio distribuzione feature e calo di metriche, che innesca retraining;
  \item \textbf{Hot-swap}: sostituzione del modello senza fermare il job streaming (richiede doppio caricamento o query restart controllato).
\end{itemize}
Nessuno di questi meccanismi è implementato; il trainer sovrascrive \texttt{ML\_MODEL\_PATH} e il predictor deve essere riavviato per caricare una nuova versione.

\subsection{Fault tolerance e exactly-once}

Il checkpoint in \texttt{STREAMING\_CHECKPOINT\_PATH} registra offset Kafka e metadati del query plan.
In caso di failure del driver, il restart riprende dall'ultimo checkpoint committato.
Checkpoint + append idempotente su Delta garantisce exactly-once a livello di micro-batch; duplicati di business key restano possibili se si ripete l'esperimento senza deduplicazione.

\section{Orchestrazione: \texttt{AppRunner}}\label{sec:app-runner}

\texttt{AppRunner} coordina l'avvio in sequenza logica:

\begin{enumerate}
  \item Verifica esistenza del modello in \texttt{ML\_MODEL\_PATH}; se assente, invoca \texttt{EcgModelTrainer.main}.
  \item Avvia \texttt{EcgStreamingPredictor} in un thread demone (\texttt{setDaemon(true)}).
  \item Attende 15 secondi (\texttt{Thread.sleep(15000)}) per il warmup di Spark Streaming.
  \item Avvia \texttt{EcgPatientSimulator}.
  \item Attende la terminazione del thread predictor (\texttt{predictorThread.join()}).
\end{enumerate}

L'attesa di 15 secondi è un'euristica che evita che i primi messaggi arrivino prima che la query streaming sia attiva; in produzione andrebbe sostituita da health-check (\texttt{isActive}, connettività Kafka, readiness del consumer group).

\texttt{AppRunner} rappresenta il punto di ingresso unico dell'applicazione (\texttt{sbt run}).
La sequenza di avvio riflette dipendenze logiche: il modello deve esistere prima che il predictor possa caricarlo; il predictor deve essere attivo prima che il simulatore inizi a pubblicare, altrimenti i primi messaggi potrebbero accumularsi nel topic senza essere consumati immediatamente.
Il thread demone del predictor garantisce che la JVM non termini quando il main thread completa il simulatore, ma resta in attesa di \texttt{awaitAnyTermination} fino a interruzione esplicita o fine dello stream.
In assenza di \texttt{join()}, il programma chiuderebbe la JVM mentre la query streaming è ancora in esecuzione, perdendo predizioni non ancora scritte su Delta.

\section{Requisiti non funzionali}\label{sec:requisiti-nf}

\subsection{Disaccoppiamento}

Kafka isola producer e consumer: il simulatore può terminare dopo 1000 messaggi mentre il predictor consuma il backlog.
Il trainer batch è indipendente dal flusso streaming, eseguibile on-demand o schedulato (cron, Airflow).

Il disaccoppiamento temporale è particolarmente utile in scenari di manutenzione: è possibile fermare il predictor per aggiornare il modello o modificare la configurazione Spark senza interrompere l'acquisizione sul dispositivo edge, che continua a pubblicare sul topic fino al raggiungimento della retention configurata.
Analogamente, un picco di carico sul predictor non si propaga al producer, che opera a velocità indipendente.

\subsection{Idempotenza e consistenza}

Delta Lake append con checkpoint Spark produce un log immutabile di predizioni.
Ripetere l'esperimento con gli stessi dati genera righe duplicate (non idempotenza su \texttt{record}); un \texttt{MERGE} Delta su chiave risolverebbe il problema.
In termini concreti, l'idempotenza della pipeline dipende dal checkpoint di Structured Streaming: se il predictor si arresta e viene riavviato, Spark riprende dall'ultimo offset committato, evitando la rielaborazione dei micro-batch già scritti su Delta.
Il log append-only di Delta Lake consente comunque di rilevare eventuali duplicati a posteriori tramite query SQL sulla colonna \texttt{record}, anche senza ricorrere a \texttt{MERGE}.
In ambito clinico, la tracciabilità completa di ogni predizione --- incluse eventuali ripetizioni --- può risultare preferibile rispetto a una deduplicazione silenziosa, poiché garantisce che nessun risultato venga scartato senza possibilità di audit.

\subsection{Scalabilità}

La scalabilità orizzontale passa dal parallelismo delle partizioni Kafka e dal numero di executor Spark.
Con topic a partizione singola, il consumo resta serializzato; aumentare partizioni e parallelismo sblocca throughput superiore, come stimato nel Capitolo~\ref{ch:analisi-dei-risultati-e-test}.
Ogni partizione Kafka viene assegnata ad al più un task Spark per micro-batch: il grado di parallelismo effettivo è quindi limitato dal minimo tra il numero di partizioni e il numero di core disponibili negli executor.
La configurazione a partizione singola adottata nel prototipo serializza il consumo, ma preserva l'ordine temporale dei messaggi all'interno della partizione, proprietà rilevante quando la sequenza dei campioni ECG ha significato clinico.
Con un partizionamento basato sull'identificativo del paziente, flussi provenienti da dispositivi diversi potrebbero essere elaborati in parallelo, mantenendo al contempo l'ordinamento per-paziente necessario alla corretta ricostruzione del segnale.

\subsection{Evolvibilità dello schema}

L'approccio JSON implicito è fragile: Delta può evolvere lo schema in append, ma \texttt{from\_json} fallisce su tipi incompatibili o campi obbligatori mancanti.
Formati schema-first (Avro, Protobuf) e Registry mitigano il rischio.
Se, ad esempio, una nuova versione del firmware aggiungesse una colonna al payload JSON, \texttt{from\_json} con lo schema precedente scarterebbe silenziosamente il campo aggiuntivo; viceversa, la rimozione di un campo produrrebbe valori null nella colonna corrispondente.
Il rischio principale è che il modello si attende 32 feature numeriche: un cambiamento dello schema potrebbe introdurre null che degradano le predizioni senza generare errori espliciti, poiché \texttt{setHandleInvalid("keep")} nel \texttt{VectorAssembler} gestisce i valori mancanti in modo silente.
Un formato schema-first intercetterebbe la discrepanza già in fase di deserializzazione, rendendo il problema visibile prima che raggiunga il modello.

\subsection{Observability}

Il prototipo si affida a log su console (\texttt{println}, \texttt{show} nei batch) e alla Spark UI locale.
In produzione servirebbero metriche esportate (throughput consumer, lag Kafka, latenza micro-batch, error rate parsing) verso Prometheus/Grafana o equivalenti cloud, con tracing distribuito per correlare \texttt{batchId} e offset.
Senza observability strutturata, diagnosticare colli di bottiglia o drift operativo resta difficile oltre la demo controllata.
A titolo di esempio, il monitoraggio del consumer lag di Kafka --- la differenza tra l'offset più recente nel topic e l'offset dell'ultimo messaggio consumato --- consentirebbe di verificare in tempo reale se il predictor mantiene il passo con l'ingestione; un lag crescente segnalerebbe backpressure o un collo di bottiglia nella fase di inferenza.
Spark espone l'interfaccia \texttt{StreamingQueryListener}, che notifica per ogni micro-batch metriche quali tempo di elaborazione, numero di righe in ingresso e aggiornamenti di stato; tali dati potrebbero essere inoltrati a un sistema di monitoraggio esterno senza alterare la logica applicativa della pipeline.

\subsection{Sicurezza}

L'istanza Kafka in Docker espone \texttt{PLAINTEXT} su localhost: nessuna autenticazione SASL, nessun TLS.
I dati ECG, anche come feature, sono informazioni sanitarie sensibili: un deployment reale richiederebbe cifratura in transito (TLS tra edge, broker e Spark), controllo accessi al topic, cifratura at-rest su Delta e policy di retention allineate al GDPR.
Il prototipo accademico omette questi controlli per ridurre la frizione sperimentale; vanno elencati esplicitamente come gap verso un sistema clinicamente deployabile.
Nel prototipo, tutto il traffico transita esclusivamente sulla rete Docker locale (localhost), circostanza che limita la superficie di attacco alla macchina host; in un deployment multi-host, il traffico Kafka non cifrato esporrebbe le feature ECG sulla rete, rendendole intercettabili.
I file Delta Lake su filesystem locale ereditano i controlli di accesso del sistema operativo, ma non dispongono di autorizzazione a grana fine --- ad esempio a livello di riga o di colonna --- che un sistema clinico richiederebbe per garantire che solo il personale autorizzato possa accedere ai dati di specifici pazienti o a determinate classi di predizioni.

\subsection{Manutenibilità e modularità}

La separazione in package \texttt{batch}, \texttt{streaming} e \texttt{utils} consente di modificare un componente senza ricompilare l'intero progetto, purché i contratti (\texttt{Config}, schema JSON, path del modello) restino stabili.
Ogni modulo espone un \texttt{main} indipendente, eseguibile separatamente per debug: il trainer senza simulatore, il predictor con messaggi già presenti nel topic, il simulatore senza consumer attivo.
Questa modularità facilita lo sviluppo incrementale e la diagnosi di failure isolati, riducendo il tempo necessario per identificare quale stadio della pipeline introduce un errore.

Il Capitolo~\ref{ch:implementazione-e-dettagli-tecnici} approfondisce configurazione e codice; il Capitolo~\ref{ch:analisi-dei-risultati-e-test} raccoglie le misure sperimentali.
